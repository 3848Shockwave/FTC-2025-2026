
===================================================================================================
                                    Blue Far Side
===================================================================================================

  public static class Paths {
    public PathChain score1;
public PathChain ready1;
public PathChain grab1;
public PathChain score2;
public PathChain ready2;
public PathChain grab2;
public PathChain score3;
public PathChain ready3;
public PathChain grab3;
public PathChain score4;

    public Paths(Follower follower) {
      score1 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(55.000, 8.000),

            new Pose(60.000, 15.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(270), Math.toRadians(300))

        .build();

ready1 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(60.000, 15.000),

            new Pose(55.000, 35.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(300), Math.toRadians(180))

        .build();

grab1 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(55.000, 35.000),

            new Pose(25.000, 35.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(180))

        .build();

score2 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(25.000, 35.000),

            new Pose(65.000, 17.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(300))

        .build();

ready2 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(65.000, 17.000),

            new Pose(65.000, 60.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(300), Math.toRadians(180))

        .build();

grab2 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(65.000, 60.000),

            new Pose(25.000, 60.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(180))

        .build();

score3 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(25.000, 60.000),

            new Pose(60.000, 84.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(320))

        .build();

ready3 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(60.000, 84.000),

            new Pose(50.000, 84.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(320), Math.toRadians(180))

        .build();

grab3 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(50.000, 84.000),

            new Pose(25.000, 84.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(180))

        .build();

score4 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(25.000, 84.000),

            new Pose(55.000, 105.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(320))

        .build();
    }
  }







====================================================================================================
                                        RED GOAL SIDE
====================================================================================================

  public static class Paths {
    public PathChain check;
public PathChain shoot1;
public PathChain ready1;
public PathChain grab1;
public PathChain score2;
public PathChain ready2;
public PathChain grab2;
public PathChain score3;
public PathChain ready3;
public PathChain grab3;
public PathChain score4;

    public Paths(Follower follower) {
      check = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(125.000, 122.000),

            new Pose(85.000, 110.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(217), Math.toRadians(270))

        .build();

shoot1 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(85.000, 110.000),

            new Pose(85.000, 96.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(270), Math.toRadians(213))

        .build();

ready1 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(85.000, 96.000),

            new Pose(85.000, 83.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(217), Math.toRadians(0))

        .build();

grab1 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(85.000, 83.000),

            new Pose(120.000, 83.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(0))

        .build();

score2 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(120.000, 83.000),

            new Pose(90.000, 90.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(217))

        .build();

ready2 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(90.000, 90.000),

            new Pose(90.000, 60.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(217), Math.toRadians(0))

        .build();

grab2 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(90.000, 60.000),

            new Pose(120.000, 60.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(0))

        .build();

score3 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(120.000, 60.000),

            new Pose(87.000, 87.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(217))

        .build();

ready3 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(87.000, 87.000),

            new Pose(87.000, 35.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(217), Math.toRadians(0))

        .build();

grab3 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(87.000, 35.000),

            new Pose(120.000, 35.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(0))

        .build();

score4 = follower.pathBuilder().addPath(
          new BezierLine(
            new Pose(120.000, 35.000),

            new Pose(80.000, 80.000)
          )
        ).setLinearHeadingInterpolation(Math.toRadians(0), Math.toRadians(217))

        .build();
    }
  }
